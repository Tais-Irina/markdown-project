#Ўпаргалка. Ќачало работы с Git

## »нициализаци€ репозитори€

`git init` (от англ. initialize, Ђинициализироватьї) Ч инициализируй репозиторий.

##—инхронизаци€ локального и удалЄнного репозиториев
`git remote add origin https://github.com/YandexPracticum/first-project.git `(от англ. remote, ЂудалЄнныйї + add, Ђдобавитьї) Ч прив€жи локальный репозиторий к удалЄнному с URL `https://github.com/YandexPracticum/first-project.git`;

`git remote -v` (от англ. verbose, Ђподробныйї) Ч проверь, что репозитории действительно св€зались;

`git push -u origin main` (от англ. push, Ђтолкатьї) Ч в первый раз загрузи все коммиты из локального репозитори€ в удалЄнный с названием origin.

¬аша ветка может называтьс€ master, а не main. ѕодправьте команду, если это необходимо.

`git push` (от англ. push, Ђтолкатьї) Ч загрузи коммиты в удалЄнный репозиторий после того, как он был прив€зан с помощью флага -u.

##ѕодготовка файла к коммиту

`git add todo.txt` (от англ. add, Ђдобавитьї) Ч подготовь файл todo.txt к коммиту;

`git add --all `(от англ. add, Ђдобавитьї + all, ЂвсЄї) Ч подготовь к коммиту сразу все файлы, в которых были изменени€, и все новые файлы;

`git add .` Ч подготовь к коммиту текущую папку и все файлы в ней.
—оздание и публикаци€ коммита

`git commit -m " омментарий к коммиту." `(от англ. commit, Ђсовершатьї, фиксироватьї + message, Ђсообщениеї) Ч сделай коммит и оставь комментарий, чтобы было проще пон€ть, какие изменени€ сделаны;

`git push `(от англ. push, Ђтолкатьї) Ч добавь изменени€ в удалЄнный репозиторий.

##ѕросмотр информации о коммитах

`git log` (от англ. log, Ђжурнал [записей]ї) Ч выведи подробную историю коммитов;

`git log --oneline `(от англ. log, Ђжурнал [записей]ї + oneline, Ђодной строкойї) Ч покажи краткую информацию о коммитах: сокращЄнный хеш и сообщение.

##ѕросмотр состо€ни€ файлов

`git status`(от англ. status, Ђстатусї, Ђсосто€ниеї) Ч покажи текущее состо€ние репозитори€.

##ƒобавление изменений в последний коммит

`git commit --amend --no-edit` (от англ. amend, Ђисправитьї) Ч добавь изменени€ к последнему коммиту и оставь сообщение прежним;

`git commit --amend -m "Ќовое сообщение"` Ч измени сообщение к последнему коммиту на Ќовое сообщение.

¬ыйти из редактора Vim: нажать Esc, ввести :qa!, нажать Enter.

##Ђќткатї файлов и коммитов

`git restore --staged hello.txt `(от англ. restore, Ђвосстановитьї) Ч переведи файл hello.txt из состо€ни€ staged обратно в untracked или modified;

`git restore hello.txt` Ч верни файл hello.txt к последней версии, котора€ была сохранена через git commit или git add;

`git reset --hard b576d89 `(от англ. reset, Ђсбросї, Ђобнулениеї + hard, Ђсуровыйї) Ч удали все незакоммиченные изменени€ из staging и Ђрабочей зоныї вплоть до указанного коммита.


##ѕросмотр изменений

`git diff `(от англ. difference, Ђотличиеї, Ђразницаї) Ч покажи изменени€ в Ђрабочей зонеї, то есть в modified-файлах;

`git diff a9928ab 11bada1 `Ч выведи разницу между двум€ коммитами;

`git diff --staged `Ч покажи изменени€, которые добавлены в staged-файлах.

¬ы с нами уже два модул€ и наверн€ка успели составить некоторое впечатление о курсе. Ќадеемс€, что вам с нами интересно!
